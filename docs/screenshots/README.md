# Screenshots

Drop the README screenshots here using these exact filenames (PNG, retina/2x if
possible, browser chrome cropped out, window ~1440px wide):

| Filename | Shot |
|---|---|
| `hero-board-light.png` | The Board, light mode — ranked cards with Fit % gauges + day nav |
| `board-dark.png`       | The Board, dark mode (for the light/dark side-by-side) |
| `job-detail.png`       | A job detail page — JD with matched terms highlighted + Fit breakdown + résumé panel |
| `insights.png`         | The Insights page — score distribution, sectors, funnel |
| `builder.png`          | The Résumé Builder — form on the left, live preview on the right |
| `run-progress.png`     | The Run button mid-pipeline with the live progress bar |
| `tracker.png`          | The Tracker — application table with editable status |
| `people.png`           | The People page — contacts grouped by company (optional) |
